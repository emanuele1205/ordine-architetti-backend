# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# SMTP Email (opzionale - se non configurato usa modalità mock che logga in console)
# Gmail: SMTP_SERVICE=gmail, SMTP_USER=tua@gmail.com, SMTP_PASS=app-password
# Mailtrap (test): SMTP_HOST=sandbox.smtp.mailtrap.io, SMTP_PORT=587, SMTP_USER=..., SMTP_PASS=...
# Outlook: SMTP_SERVICE=hotmail, SMTP_USER=tua@outlook.com, SMTP_PASS=password
SMTP_SERVICE=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM_NAME=Ordine Architetti Caltanissetta

# URL frontend (per link nelle email)
FRONTEND_URL=http://localhost:3000

# Server
PORT=5000
NODE_ENV=development
